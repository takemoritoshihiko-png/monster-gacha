# Deferred Backlog — monster-gacha (TREASURE GACHA)

追記型。対応したら行を更新する(削除しない)。

## 2026-08-24 ゴッドアナザー追加時の保留事項

### 裁定待ち(竹森氏の判断が必要)
1. **当たりゼロのプレイで所持コインが純減する**: → **裁定済み(2026-08-24 竹森氏): 案C=現状維持で確定**。マイナス終了はパチスロらしさとして仕様。以後この件は変更しない(変更には再裁定が必要)

### 既存バグ(今回の変更起因ではない・実測確認済み 2026-08-24)
2. **JugglerGame の `stopMainBgm()` は ReferenceError**(index.html 7094行付近): STARTのたびconsoleに例外が出る。状態更新が先に完了するためゲームは正常起動する(ブラウザ実測で確認)。修正は該当呼び出しの削除のみ。
3. **JugglerGame 内の未使用 `renderCRT`**(未定義変数`G`参照・呼び出しなしのデッドコード)。
4. **CRT(ぷちゅん)演出のレンダー時刻依存**: ジャグラー側は再描画が1秒刻みのため縮小アニメがほぼ見えない(ゴッドアナザー側は2026-08-24にRAF tickで修正済み)。

### 軽微(ゴッドアナザー)
5. 連チャンバナーの倍率表示が1G連の+1.0を含まない(払い出しメッセージ「×2.1」とバナー「×1.1」が同時に見える)。バナーの意味論を決めて統一する。
6. 結果画面の「中段黄7 N回」「レアSIN N回」は的中回数のみ(ガセ・ハズレは非計上)。成立回数を出すなら別カウンタ要。
7. 演出ロック中にタブをバックグラウンドへ移すと、凍結がフレームあたり最大500msしか効かず離脱実時間ぶん残り時間が減る(通常プレイでは影響なし)。
8. `computeJug` の `streak1G` は計算のみで未使用(デッドコード)。
9. `onTouchStart` の `preventDefault()` はReact18のpassiveリスナでは無効(console警告のみ・スクロール抑止はtouchActionが担保)。ジャグラーと同型。
10. GOD当選時の効果音生成(5秒バッファ同期合成)は低スペック端末で一瞬フレーム落ちの可能性(ジャグラーと同型・実測未)。

## 2026-08-24 アセット化(Gemini生成)時の保留事項
11. **アメイジングGOD動画の実画面最終確認が未了**: video要素の再生不能バグ(React要素再生成)は根治済み(key付与)・play()成功も実測済みだが、演出全体の見え方は実プレイ未確認。次にアメイジング(GOD 100G以内連チャン)を引いた時に確認。長すぎる場合はロック延長定数(直GOD 6200ms/クラッシュ8300ms)を縮める
12. **新サウンド(レバー/回転/停止)とファンファーレの試聴検品が未了**(AIは音を聞けない)。調整点は sndTap 第3引数と回転ループの2つのgain
13. カットイン表示中に巨大GOD文字(z6)がカットイン(z5)の上に重なる。汚く見える場合はz値1箇所の変更で調整可
14. Gemini生成画像に可視透かしは無かった(実測)。Veo動画の透かし有無は実プレイ時に要確認(あればクロップ再エンコード=要ffmpeg)
15. 新規要望キューの残り: プレイ画面の計器盤背景へのAI装飾帯適用(現状はSVG角飾りのみ)

## 2026-08-24 スロット本体プロ化+操作改善の保留事項
16. ベゼル適用中はランプ演出の「枠の色変化」が見えない(枠色はborder-imageに上書きされる。外周グローとリング発光は生きている)。物足りなければ調整
17. 図柄画像のscreen合成はストリップ内に閉じるため、当たり図柄の「後光」がやや弱く見える可能性(実見で気になれば後光をストリップ内へ移す)
18. 宝箱の木・銀一括開封により開封音が1グループ1回に(旧はランク毎)。音が寂しければ要調整
19. 2本指交互連打の理論上限は25タップ/秒(80ms窓内2受理)。収支上振れが気になる場合は窓内受理数か確率の再調整を裁定

## 2026-08-24 敵対的全面検査の残課題(修正済み分はコミットログ参照)
20. 【裁定】デイリーミッションの1人あたり上限(target/2)により、1人で遊び続けると途中で貢献が止まる(例: ミニゲーム150回→75で停止)。撤廃/緩和/仕様維持の裁定待ち
21. 【裁定】ミニゲームのマイナススコアが財布から引かれる+ココミ1.3倍は損失も1.3倍(ゴッドアナザーは裁定済み現状維持。全ゲーム共通の下限0クランプを入れるかは別裁定)
22. restoreDataに collection が無い場合ランキング更新が恒久停止する理論リスク(現在は全員nullで未発生)。復旧機構を使う際は要注意
23. Firebase書込の大半に.catchがなく失敗が無言(今回は主要経路のみ対処)。全書込への網掛けは次回
24. ゲーム履歴・ランキング画面はmount時1回読みで開きっぱなしでは更新されない
25. 深夜0時直後の最大30秒、ミッション貢献が画面に反映されない表示窓(データは正しい日付に入る)
26. dailyRankings/weeklyRankings/assetTracking等の日付キーが無限成長(掃除処理なし)
27. ミニゲーム個別ミッション対象が5種のみ(godAnother等13種はminigame累計のみ)。意図の確認待ち
28. pull()のdepsにcrownBonus欠落/GACHA_COST_30の名前が40連実態と不一致/裏アイテム複数当選時に2個目以降が無言付与/ベゼルON時の内側角丸不整合 ほか軽微
