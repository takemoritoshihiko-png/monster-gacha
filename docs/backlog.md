# Deferred Backlog — monster-gacha (TREASURE GACHA)

追記型。対応したら行を更新する(削除しない)。

## 2026-08-24 ゴッドアナザー追加時の保留事項

### 裁定待ち(竹森氏の判断が必要)
1. **当たりゼロのプレイで所持コインが純減する**: ゴッドアナザーは1回転−1枚のため、当たり無しで終わると最終スコアがマイナスになり財布から引かれる。実タップ速度(秒3回)だと約28%のプレイが−500枚前後で終わる想定(コードから算出)。ジャグラーでは実質起きない(合算1/98)ため新規リスク。対応案 A: 終了時スコアを下限0にクランプ / B: 中段黄7を1/468→1/164に上げる(期待値再計算要) / C: パチスロらしさとして仕様のまま。→ **裁定待ち**

### 既存バグ(今回の変更起因ではない・実測確認済み 2026-08-24)
2. **JugglerGame の `stopMainBgm()` は ReferenceError**(index.html 7094行付近): STARTのたびconsoleに例外が出る。状態更新が先に完了するためゲームは正常起動する(ブラウザ実測で確認)。修正は該当呼び出しの削除のみ。
3. **JugglerGame 内の未使用 `renderCRT`**(未定義変数`G`参照・呼び出しなしのデッドコード)。
4. **CRT(ぷちゅん)演出のレンダー時刻依存**: ジャグラー側は再描画が1秒刻みのため縮小アニメがほぼ見えない(ゴッドアナザー側は2026-08-24にRAF tickで修正済み)。

### 軽微(ゴッドアナザー)
5. 連チャンバナーの倍率表示が1G連の+1.0を含まない(払い出しメッセージ「×2.1」とバナー「×1.1」が同時に見える)。バナーの意味論を決めて統一する。
6. 結果画面の「中段黄7 N回」「レアSIN N回」は的中回数のみ(ガセ・ハズレは非計上)。成立回数を出すなら別カウンタ要。
7. 演出ロック中にタブをバックグラウンドへ移すと、凍結がフレームあたり最大500msしか効かず離脱実時間ぶん残り時間が減る(通常プレイでは影響なし)。
8. `computeJug` の `streak1G` は計算のみで未使用(デッドコード)。
9. `onTouchStart` の `preventDefault()` はReact18のpassiveリスナでは無効(console警告のみ・スクロール抑止はtouchActionが担保)。ジャグラーと同型。
10. GOD当選時の効果音生成(5秒バッファ同期合成)は低スペック端末で一瞬フレーム落ちの可能性(ジャグラーと同型・実測未)。
